# 0.6 New note in Single Page App

```mermaid
sequenceDiagram
    participant Browser
    participant Server

    Note over Browser: The browser sends a new note as JSON data to the server
    Note over Browser: {content: "fullstackopen",<br/> date: "2025-12-28"}

    Browser->>+Server: HTTP POST /new_note_spa

    Note over Server: This time the server does not redirect the browser<br/>and it sends no further HTTP requests

    Server-->>-Browser: HTTP 201 Created

    Note over Browser: The browser doesn't reload the page
    Note over Browser: The browser executes an event handler<br/>that renders notes to display

